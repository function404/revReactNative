import { View } from "react-native";
import { Text } from "react-native-paper";
import styles from "../utils/style";

const EditTask = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Edit Task Screen</Text>
    </View>
  );
};

export default EditTask;